export enum AppRoute {
    Catalog = '/',
    Product ='/product',
    NotFound = '*',
  }

  export const TIMEOUT_SHOW_ERROR = 2000;
  
  export enum NameSpace {
    Catalog = 'CATALOG',
    Error = 'ERROR'
  }

  export enum APIRoute {
    Cameras = '/cameras',
  }